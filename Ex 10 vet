#include <stdio.h>

int main () {
    float n[15], media, soma=0;
    for (int i=0; i<15; i++){
        printf ("digite a nota:");
        scanf ("%f", &n[i]);
        soma += n[i];
    }
    media = soma/15;
    printf ("a media da turma é: %.2f", media);
}
